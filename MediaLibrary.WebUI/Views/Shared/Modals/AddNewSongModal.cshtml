@model AddNewSongModalViewModel

@using (Html.BeginForm(nameof(MusicController.Upload), "Music", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="modal fade" id="@(HtmlControlsRepository.NewSongModalId)" role="dialog" aria-labelledby="modalAddNewSongTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable" role="document">
            <div class="modal-content" style="max-height: 75%">
                <div class="modal-header">
                    <h5 class="modal-title" id="modalAddNewSongTitle">Upload Song</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body d-flex flex-column">
                    <div class="mb-3">
                        @Html.TextBoxFor(x => x.MusicFile, null, new { @class = "form-control form-control-sm", type = "file", accept = ".mp3" })
                    </div>
                    @Html.HiddenFor(x => x.MusicPath, new { data_field = "MusicPath" })
                    <div class="flex-fill overflow-auto" data-container="NewSongDirectorySelector"></div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-primary" data-song-action="upload" data-bs-tooltip="tooltip" title="Upload song">Upload</button>
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal" data-bs-tooltip="tooltip" title="Cancel">Cancel</button>
                </div>
            </div>
        </div>
    </div>
}